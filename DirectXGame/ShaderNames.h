#pragma once

#include "Prerequisites.h"

using namespace std;

class ShaderNames
{
public:
	static const unordered_map<string, wstring> PixelShaderNames;
	static const unordered_map<string, wstring> VertexShaderNames;
};